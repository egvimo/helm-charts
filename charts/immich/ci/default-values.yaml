immich:
  controllers:
    main:
      containers:
        main:
          env:
            DB_HOSTNAME: cnpg-cluster-rw
            DB_DATABASE_NAME: test
            DB_USERNAME: test
            DB_PASSWORD: test
