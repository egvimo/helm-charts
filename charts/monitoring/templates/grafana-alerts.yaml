apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-grafana-node-alerts
  labels:
    grafana_alert: "1"
data:
  rules.yaml: |-
    {{- .Files.Get "alerts/node.yaml" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-grafana-pod-alerts
  labels:
    grafana_alert: "1"
data:
  rules.yaml: |-
    {{- .Files.Get "alerts/pod.yaml" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-grafana-argocd-alerts
  labels:
    grafana_alert: "1"
data:
  rules.yaml: |-
    {{- .Files.Get "alerts/argocd.yaml" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-grafana-longhorn-alerts
  labels:
    grafana_alert: "1"
data:
  rules.yaml: |-
    {{- .Files.Get "alerts/longhorn.yaml" | nindent 4 }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "common.fullname" . }}-home-assistant-alerts
  labels:
    grafana_alert: "1"
data:
  rules.yaml: |-
    {{- .Files.Get "alerts/home-assistant.yaml" | nindent 4 }}
