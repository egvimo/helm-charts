apiVersion: v1
kind: Service
metadata:
  name: {{ include "jdownloader.fullname" . }}
  labels:
    {{- include "jdownloader.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: direct-conn
      protocol: TCP
      name: direct-conn
  selector:
    {{- include "jdownloader.selectorLabels" . | nindent 4 }}
